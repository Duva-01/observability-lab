\documentclass[
$for(classoption)$
$classoption$$sep$,
$endfor$
]{$documentclass$}

$if(lang)$
\usepackage[$lang$]{babel}
$endif$
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{hyperref}
\usepackage[pdftex]{xcolor}
\usepackage{longtable,booktabs,array}
\usepackage{calc}
\usepackage{graphicx}
\usepackage{float}
\usepackage{enumitem}
\usepackage{framed}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{tocloft}
\usepackage{microtype}

$for(geometry)$
\geometry{$geometry$}
$endfor$

$if(linestretch)$
\setstretch{$linestretch$}
$endif$

$if(colorlinks)$
\hypersetup{
  colorlinks=true,
  linkcolor=$if(linkcolor)$$linkcolor$$else$black$endif$,
  urlcolor=$if(urlcolor)$$urlcolor$$else$blue$endif$,
  pdftitle={$title$},
  pdfauthor={$for(author)$$author$$sep$; $endfor$}
}
$else$
\hypersetup{
  hidelinks,
  pdftitle={$title$},
  pdfauthor={$for(author)$$author$$sep$; $endfor$}
}
$endif$

$for(header-includes)$
$header-includes$
$endfor$

\begin{document}

\begin{titlepage}
  \thispagestyle{empty}
  \vspace*{2.2cm}

  {\large\scshape Memoria técnica\par}
  \vspace{1.6cm}

  {\Huge\bfseries $title$\par}
  \vspace{0.8cm}

$if(subtitle)$
  {\Large $subtitle$\par}
  \vspace{1.4cm}
$endif$

  {\large Diseño, implementación e integración de métricas, logs, trazas, despliegue cloud native y automatización de plataforma\par}
  \vspace{2.0cm}

  {\large\textbf{Autor:} $for(author)$$author$$sep$; $endfor$\par}
  \vspace{0.25cm}
$if(date)$
  {\large\textbf{Fecha:} $date$\par}
  \vspace{0.25cm}
$endif$
  {\large\textbf{Stack:} Spring Boot, Kubernetes, Helm, Prometheus, Grafana, OpenTelemetry, Jaeger, Elasticsearch, Kibana, GitLab CI/CD\par}

  \vfill
\end{titlepage}

\pagenumbering{arabic}
\setcounter{page}{1}

$if(toc)$
{
  \hypersetup{linkcolor=black}
  \tableofcontents
}
\clearpage
$endif$

$body$

\end{document}
